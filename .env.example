# GitHub Token (Optional)
# Erstelle einen Personal Access Token unter: https://github.com/settings/tokens
# Keine besonderen Berechtigungen erforderlich für öffentliche Repositories
# Für private Repositories: 'repo' Berechtigung erforderlich
GITHUB_TOKEN=

# Port (Standard: 3000)
PORT=3000

# Datenbank-Pfad (Standard: ./data/issues.db)
DB_PATH=/app/data/issues.db
